// Here I export the env variables which you'll use inside the code
console.log(`${process.env.VUE_APP_API_HOST}:${process.env.VUE_APP_API_PORT}`);
export default `${process.env.VUE_APP_API_HOST}:${process.env.VUE_APP_API_PORT}`;
